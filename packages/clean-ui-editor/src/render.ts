/**
 * The render adapter and viewer component (FR37–FR40) land in Phase 06 —
 * see docs/plans/markdown-editor-phase-1/phase-06/. This subpath exists from
 * Phase 01 so its own build entry — and the "importing the barrel pulls in
 * neither the renderer nor the sanitiser" guarantee (FR36) — can be
 * verified mechanically before anything is built on top of it.
 */
export {};
