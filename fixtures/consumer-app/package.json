{
  "name": "clean-ui-consumer-fixture",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Not a workspace member. Installed with npm from packed tarballs by scripts/verify-fixture.mjs — the only place this repo exercises a package's exports map, peer resolution and published file list as a real external consumer would.",
  "scripts": {
    "build": "vue-tsc --noEmit && vite build"
  },
  "dependencies": {
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.0",
    "typescript": "^5.7.0",
    "vite": "^6.3.0",
    "vue-tsc": "^2.2.0"
  }
}
